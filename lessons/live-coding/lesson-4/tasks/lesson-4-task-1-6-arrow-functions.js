/**
 * Задача 1.6: Стрелочные функции
 * 
 * Перепишите обычные функции в стрелочные и используйте их:
 * 
 * 1. Перепишите функцию add в стрелочную
 * 2. Перепишите функцию multiply в стрелочную
 * 3. Создайте стрелочную функцию для проверки четности числа
 * 4. Создайте стрелочную функцию для получения длины строки
 * 5. Используйте стрелочные функции в методах массивов
 * 6. Создайте стрелочную функцию для фильтрации положительных чисел
 * 
 * Пример результата:
 * Сумма: 15
 * Произведение: 50
 * 10 четное? true
 * Длина строки "Привет": 6
 * Положительные числа: [1, 5, 10]
 * Квадраты чисел: [1, 4, 9, 16, 25]
 */

const numbers = [1, 2, 3, 4, 5, -1, -5, 10];

// Ваш код здесь:

// 1. Переписать функцию add в стрелочную
const add = /* ваш код */;

// 2. Переписать функцию multiply в стрелочную
const multiply = /* ваш код */;

// 3. Стрелочная функция для проверки четности
const isEven = /* ваш код */;

// 4. Стрелочная функция для получения длины строки
const getLength = /* ваш код */;

// 5. Использовать стрелочные функции в методах массивов
const positiveNumbers = numbers.filter(/* ваш код */);
const squares = numbers.map(/* ваш код */);

// Проверка результатов
console.log('Сумма:', add(5, 10));
console.log('Произведение:', multiply(5, 10));
console.log('10 четное?', isEven(10));
console.log('Длина строки "Привет":', getLength('Привет'));
console.log('Положительные числа:', positiveNumbers);
console.log('Квадраты чисел:', squares);
