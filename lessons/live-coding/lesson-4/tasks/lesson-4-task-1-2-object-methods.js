/**
 * Задача 1.2: Методы объектов
 * 
 * У вас есть объект пользователя:
 * const user = {
 *   name: 'Иван',
 *   age: 25,
 *   city: 'Москва',
 *   skills: ['JavaScript', 'HTML', 'CSS']
 * };
 * 
 * Выполните следующие операции:
 * 
 * 1. Используйте Object.keys() чтобы получить массив ключей
 * 2. Используйте Object.values() чтобы получить массив значений
 * 3. Используйте Object.entries() чтобы получить массив пар [ключ, значение]
 * 4. Проверьте, есть ли ключ 'email' в объекте
 * 5. Добавьте новое свойство 'experience: 2' к объекту
 * 
 * Пример результата:
 * Ключи: ['name', 'age', 'city', 'skills']
 * Значения: ['Иван', 25, 'Москва', ['JavaScript', 'HTML', 'CSS']]
 * Пары: [['name', 'Иван'], ['age', 25], ...]
 */

const user = {
  name: 'Иван',
  age: 25,
  city: 'Москва',
  skills: ['JavaScript', 'HTML', 'CSS']
};

// Ваш код здесь:

// 1. Получить массив ключей
const keys = /* ваш код */;

// 2. Получить массив значений
const values = /* ваш код */;

// 3. Получить массив пар [ключ, значение]
const entries = /* ваш код */;

// 4. Проверить наличие ключа 'email'
const hasEmail = /* ваш код */;

// 5. Добавить новое свойство
/* ваш код */

// Проверка результатов
console.log('Ключи:', keys);
console.log('Значения:', values);
console.log('Пары:', entries);
console.log('Есть email?', hasEmail);
console.log('Обновленный пользователь:', user);
