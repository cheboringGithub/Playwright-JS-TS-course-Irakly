/**
 * Задача 1.3: Деструктуризация
 * 
 * У вас есть объект продукта:
 * const product = {
 *   id: 1,
 *   name: 'Телефон',
 *   price: 25000,
 *   brand: 'Apple',
 *   colors: ['черный', 'белый', 'золотой']
 * };
 * 
 * Выполните следующие операции с помощью деструктуризации:
 * 
 * 1. Извлеките name, price и brand в отдельные переменные
 * 2. Извлеките первый цвет из массива colors
 * 3. Создайте новый объект с только name и price
 * 4. Извлеките остальные цвета (кроме первого) в массив
 * 5. Создайте функцию, которая принимает объект и возвращает строку с информацией
 * 
 * Пример результата:
 * Название: Телефон
 * Цена: 25000
 * Бренд: Apple
 * Первый цвет: черный
 * Остальные цвета: ['белый', 'золотой']
 */

const product = {
  id: 1,
  name: 'Телефон',
  price: 25000,
  brand: 'Apple',
  colors: ['черный', 'белый', 'золотой']
};

// Ваш код здесь:

// 1. Извлечь name, price и brand
const { /* ваш код */ } = product;

// 2. Извлечь первый цвет
const [ /* ваш код */ ] = product.colors;

// 3. Создать новый объект с name и price
const productInfo = { /* ваш код */ };

// 4. Извлечь остальные цвета
const [ /* ваш код */ ] = product.colors;

// 5. Функция для вывода информации
function getProductInfo(/* ваш код */) {
  /* ваш код */
}

// Проверка результатов
console.log('Название:', name);
console.log('Цена:', price);
console.log('Бренд:', brand);
console.log('Первый цвет:', firstColor);
console.log('Остальные цвета:', otherColors);
console.log('Информация о продукте:', productInfo);
console.log('Строка информации:', getProductInfo(product));
